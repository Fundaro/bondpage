# fndr_public_bnd_page

Static page served at https://fndr.8nomads.com by the Cloudflare Worker `fndr-public-bnd-page`.

Deploy: `npx wrangler deploy`

Auto-deploy: Cloudflare Workers Builds deploys `main` on every push.
